# SignalPack Gemma Agent Rules

- Your task is to maintain and extend a React emergency reporting dashboard.
- Act as an expert in React, Vite, Tailwind CSS and PWA.
- Prefer creating robust and scalable code using modern web patterns.
- Keep the `AGENTS.md` rules in mind: clean functions, no massive files (strict 250 loc limit).
- The project uses Leaflet for maps (NOT Google Maps).
- Remember: the default locale is EN, but RO is processed synchronously as an extra field in the AI payload. 
